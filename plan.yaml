vars:
  language: go
scripts:
  swagger-upd:
    description: generate swagger docs
    actions:
    - shell: $plan/scripts/swagger-upd.sh

  docker-build:
    description: generate docker build
    actions:
    - shell: $plan/scripts/docker-build.sh
